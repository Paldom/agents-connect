# Copy to functions/.env.<project-id> (gitignored). Values are deployed as function parameters.
APP_URL=https://<project-id>.web.app
ALLOWED_EMAILS=
SMTP_URL=
SMTP_FROM=agents-connect <no-reply@example.com>
